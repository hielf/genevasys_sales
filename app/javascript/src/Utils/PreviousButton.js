import React from 'react';
import { Button, Radio } from 'antd';


const PreviousButton = (props) => (
  <Button shape='default' size='large' href='#' block>Prev</Button>
);

export default PreviousButton;
