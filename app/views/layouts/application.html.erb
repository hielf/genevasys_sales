<!DOCTYPE html>
<html>
  <head>
    <title>Geneva Systems Ltd.</title>
    <meta name='viewport' content='width=device-width, height=device-height, user-scalable=no' />
    <link rel="icon" href="/images/favicon.ico">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <% p "is #{browser.name} on #{browser.platform.name} #{browser.platform.version}" %>
    <% if browser.device.mobile? %>
      <%= javascript_pack_tag 'applicationMobile', 'data-turbolinks-track': 'reload' %>
      <%= javascript_pack_tag 'mobile' %>
    <% else %>
      <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
      <%= javascript_pack_tag 'react' %>
    <% end %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
